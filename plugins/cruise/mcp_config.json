{
  "mcpServers": {
    "cruise": {
      "serverUrl": "${CRUISE_BASE_URL:-https://cruise.bytesbrains.net}/mcp",
      "headers": {
        "Authorization": "Bearer ${CRUISE_API_KEY}"
      }
    }
  }
}
